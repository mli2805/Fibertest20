<Window x:Class="Iit.Fibertest.WpfClient.Views.UpdateFiberView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="UpdateFiberView" SizeToContent="Height"  Width="300">
    <StackPanel>
        <TextBlock Margin="5" Text="Gps Length" />
        <TextBlock Margin="5" Text="{Binding GpsLength}" />
        <TextBlock Margin="5" Text="Optical Length" />
        <TextBlock Margin="5" Text="{Binding OpticalLength}" />
        <TextBlock Margin="5" Text="User Length" />
        <TextBox Margin="5" Text="{Binding UserInputedLength, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel Orientation="Horizontal" Margin="5,15">
            <Button Name="Save" Height="25" Width="100" Margin="5" Content="Сохранить" IsEnabled="{Binding IsButtonSaveEnabled}"/>
            <Button Name="Cancel" Height="25" Width="100" Content="Отменить"/>
        </StackPanel>
    </StackPanel>
</Window>
