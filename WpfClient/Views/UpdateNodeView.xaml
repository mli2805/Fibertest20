<Window x:Class="Iit.Fibertest.WpfClient.Views.UpdateNodeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Iit.Fibertest.WpfClient.Views"
        mc:Ignorable="d"
        Title="UpdateNodeView" Height="300" Width="300">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="Id" />
        <TextBlock Text="{Binding Id}" />
        <TextBlock Text="Title" />
        <TextBox Text="{Binding Title}" />
        <Button Name="Save" Height="25" Width="120" >Save</Button>

    </StackPanel>
</Window>
