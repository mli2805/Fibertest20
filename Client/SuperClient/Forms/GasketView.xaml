<UserControl x:Class="Iit.Fibertest.SuperClient.GasketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DataContext="{d:DesignData GasketViewModel}" Background="WhiteSmoke">
    <TabControl ItemsSource="{Binding Children}" Visibility="{Binding TabVisibility}"
                SelectedItem="{Binding SelectedTabItem, Mode=TwoWay}" />

</UserControl>
