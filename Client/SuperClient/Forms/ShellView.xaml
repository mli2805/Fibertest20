<Window x:Class="Iit.Fibertest.SuperClient.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        xmlns:sc="clr-namespace:Iit.Fibertest.SuperClient"
        WindowState="Maximized" d:DataContext="{d:DesignInstance sc:ShellViewModel}">
    <DockPanel >
        <DockPanel DockPanel.Dock="Left">
            <ToolBarTray DockPanel.Dock="Top">
                <ToolBar>
                    <Button Name="ShowClientVersions" Content="Show client version"></Button>
                </ToolBar>
            </ToolBarTray>
            <sc:ServersView DataContext="{Binding ServersViewModel}"/>
        </DockPanel>
        <sc:GasketView DataContext="{Binding GasketViewModel}"/>
    </DockPanel>

</Window>