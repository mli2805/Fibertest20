<Window x:Class="WcfTestBench.RtuState.RtuStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:rtuState="clr-namespace:WcfTestBench.RtuState"
        mc:Ignorable="d"
        Height="200" Width="600" d:DataContext="{d:DesignInstance rtuState:RtuStateViewModel}">
    <Window.Resources>
        <Storyboard x:Key="ToggleColorStoryboard">
            <ColorAnimation Storyboard.TargetName="TextBlockForeground" Storyboard.TargetProperty="Color"
                            From="Black" To="White" AutoReverse="True"
                            BeginTime="0:0:0" Duration="0:0:0.5" RepeatBehavior="Forever"/>
        </Storyboard>
    </Window.Resources>

    <StackPanel>
        <TextBlock Text="{Binding CurrentState}" Margin="5,25" TextAlignment="Center" FontSize="16" FontWeight="Bold">
            <TextBlock.Foreground>
                <SolidColorBrush x:Name="TextBlockForeground"/>
            </TextBlock.Foreground>
            <TextBlock.Triggers>
                <EventTrigger  RoutedEvent="Window.Loaded">
                    <BeginStoryboard Storyboard="{StaticResource ToggleColorStoryboard}"/>
                </EventTrigger>
            </TextBlock.Triggers>
        </TextBlock>
    </StackPanel>
</Window>
