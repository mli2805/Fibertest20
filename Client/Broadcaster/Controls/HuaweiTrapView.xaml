<UserControl x:Class="Broadcaster.HuaweiTrapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             cal:Bind.ModelWithoutContext="{Binding .}">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Text="Количество трапов" Margin="5"/>
            <TextBox Text="{Binding TrapCount}" Margin="5" Width="50"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBlock Text="Между трапами пауза, мс" Margin="5"/>
            <TextBox Text="{Binding PauseMs}" Margin="5" Width="50"/>
        </StackPanel>
        <Button Width="200" Height="22" Content="Send" Margin="5" Name="Send"/>

</StackPanel>
</UserControl>
