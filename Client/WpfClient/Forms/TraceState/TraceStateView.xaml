<Window x:Class="Iit.Fibertest.Client.TraceStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:client="clr-namespace:Iit.Fibertest.Client"
        mc:Ignorable="d" Width="400" SizeToContent="Height"
        d:DataContext="{d:DesignInstance client:TraceStateViewModel}">
    <StackPanel>
        <GroupBox Header="Info" Margin="5">
            <StackPanel>
                <TextBlock Text="{Binding Model.TraceTitle}" TextAlignment="Center"/>
                <TextBlock Text="{Binding Model.RtuTitle}"/>
                <TextBlock Text="{Binding Model.PortTitle}"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="State" Margin="5">
            <StackPanel>
                <TextBlock Text="{Binding Model.TraceState}" FontSize="16" FontWeight="Bold" 
                           Foreground="{Binding Model.TraceStateBrush}" TextAlignment="Center" />
                <Button Content="Turn alarm off/on" Margin="5"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Events" Margin="5">
            <DataGrid>

            </DataGrid>
        </GroupBox>
        <GroupBox Header="Status" Margin="5">
            <StackPanel>
                <ComboBox Margin="5"/>
                <TextBox Margin="5"/>
            </StackPanel>
        </GroupBox>
        <StackPanel>
            <Button Content="Statistics" Width="120" Height="25"/>
        </StackPanel>
    </StackPanel>

</Window>
