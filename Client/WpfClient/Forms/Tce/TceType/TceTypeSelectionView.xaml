<UserControl x:Class="Iit.Fibertest.Client.TceTypeSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:client="clr-namespace:Iit.Fibertest.Client"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance client:TceTypeSelectionViewModel}">
    <ListView DockPanel.Dock="Top" ItemsSource="{Binding TceTypes}" 
              SelectedItem="{Binding SelectedType}" Margin="1" >
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Title" Width="120" DisplayMemberBinding="{Binding Title}"/>
                <GridViewColumn Header="SoftwareVersion" Width="240" DisplayMemberBinding="{Binding SoftwareVersion}"/>
                <GridViewColumn Header="SlotCount" Width="80" DisplayMemberBinding="{Binding SlotCount}"/>
                <GridViewColumn Header="Comment" Width="240" DisplayMemberBinding="{Binding Comment}"/>
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
