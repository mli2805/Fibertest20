<UserControl x:Class="Iit.Fibertest.Client.TceSlotsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:client="clr-namespace:Iit.Fibertest.Client"
             mc:Ignorable="d" 
             Width="1160" Height="560"
             d:DataContext="{d:DesignInstance client:TceSlotsViewModel}">
    <TabControl DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding Slots}" 
                SelectedItem="{Binding SelectedSlot, Mode=TwoWay}">
        <TabControl.ItemTemplate>
            <DataTemplate DataType="client:RelationsOfSlotViewModel">
                <DockPanel>
                    <TextBlock Text="{Binding Title}" Margin="10,0"/>
                </DockPanel>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate DataType="client:RelationsOfSlotViewModel">
                <client:RelationsOfSlotView/>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>
