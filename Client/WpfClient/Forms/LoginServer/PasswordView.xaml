<UserControl x:Class="Iit.Fibertest.Client.PasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
             xmlns:wpfCommonViews="clr-namespace:Iit.Fibertest.WpfCommonViews;assembly=Iit.Fibertest.WpfCommonViews"
             xmlns:client="clr-namespace:Iit.Fibertest.Client"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" d:DesignHeight="35" d:DesignWidth="200"
             d:DataContext="{d:DesignInstance client:PasswordViewModel}"
             cal:Bind.ModelWithoutContext="{Binding .}"
             >
<Grid Height="40" VerticalAlignment="Center">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="34"/>
    </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{x:Static stringResources:Resources.SID_Password}" 
                   VerticalAlignment="Center" Margin="5,5,10,5"/>
        <Grid Grid.Column="1"  Height="22">
            <TextBox Name="PwdTextBox"  Text="{Binding Password}" VerticalContentAlignment="Center"/>
            <PasswordBox Name="PwdBox" 
                         wpfCommonViews:PasswordHelper.Attach="True"  
                         wpfCommonViews:PasswordHelper.Password="{Binding Password, Mode=TwoWay,
                                                                            UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         Width="100"/>
        </Grid>
        <Button Grid.Column="2"  Name="ChangeVisibility" Margin="5" Background="Transparent" BorderThickness="0"
                PreviewMouseLeftButtonDown="ShowPassword" PreviewMouseLeftButtonUp="HidePassword">
            <Image Source="../../Resources/Buttons/eye1.png" />
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                      Width="22" Margin="0,0,0,0"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                              Width="26"  Margin="0,0,0,0"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

    </Grid>

  
</UserControl>
