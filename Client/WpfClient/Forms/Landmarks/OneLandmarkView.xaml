<UserControl x:Class="Iit.Fibertest.Client.OneLandmarkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
             xmlns:client="clr-namespace:Iit.Fibertest.Client"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance client:OneLandmarkViewModel}"
             cal:Bind.ModelWithoutContext="{Binding .}" >
    <GroupBox DockPanel.Dock="Bottom" Header="{x:Static stringResources:Resources.SID_Landmark}" Margin="5,0,5,15">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <GroupBox Header="{x:Static stringResources:Resources.SID_Node}" Margin="5,0" DockPanel.Dock="Top" IsEnabled="{Binding IsEditEnabled}">
                <StackPanel>
                    <TextBox Margin="5" Height="25" Text="{Binding SelectedLandmark.NodeTitle}"/>
                    <TextBox Margin="5" Height="25" Text="{Binding SelectedLandmark.NodeComment}" />
                </StackPanel>
            </GroupBox>
            <GroupBox Grid.Column="1"  Grid.Row="0"  Header="{x:Static stringResources:Resources.SID_Equipment}" Margin="5,0" IsEnabled ="{Binding IsEquipmentEnabled}">
                <StackPanel>
                    <TextBox Margin="5" Height="25" Text="{Binding SelectedLandmark.EquipmentTitle}"/>
                    <ComboBox Margin="5" Height="25" ItemsSource="{Binding ComboItems}" SelectedItem="{Binding SelectedEquipmentTypeItem}" />
                </StackPanel>
            </GroupBox>
            <GroupBox Grid.Row="0"  Grid.Column="2" Header="GPS coordinates"  IsEnabled="{Binding IsEditEnabled}">
                <client:GpsInputSmallView DataContext="{Binding GpsInputSmallViewModel}"/>
            </GroupBox>
            <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" Margin="5,5,0,0" Orientation="Horizontal">
                <Button Width="140" Height="25" Margin="5" Name="Apply" Content="{x:Static stringResources:Resources.SID_Apply}" IsEnabled="{Binding IsEditEnabled}"/>
                <Button Width="140" Height="25" Margin="5" Name="Cancel" Content="{x:Static stringResources:Resources.SID_Cancel_changes}" IsEnabled="{Binding IsEditEnabled}"/>
                <Button Width="140" Height="25" Margin="5" Name="ShowLandmarkOnMap" Content="{x:Static stringResources:Resources.SID_Show_on_map}"/>
                <Button Width="140" Height="25" Margin="5" Name="ShowReflectogram" Content="{x:Static stringResources:Resources.SID_Reflectogram}" IsEnabled="{Binding IsFromBaseRef}"/>
            </StackPanel>
        </Grid>
    </GroupBox>

</UserControl>
