<Window x:Class="Iit.Fibertest.Client.RtuToZonesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
        mc:Ignorable="d"
        Title="RtuToZonesView" Height="450" Width="700" d:DataContext="{d:DesignData RtuToZonesViewModel}">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Margin="5,10" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Content="{x:Static stringResources:Resources.SID_Save}" Name="Save" IsDefault="True" Width="100" Height="20" Margin="5" />
            <Button Content="Go to responsibility zones for traces form" Name="GoToTraces" Width="300" Height="20" Margin="5"/>
            <Button Content="{x:Static stringResources:Resources.SID_Cancel}" Name="Cancel" IsCancel="True" Width="100" Height="20" Margin="5"/>
        </StackPanel>
        <DataGrid Name="MyDatagrid" ItemsSource="{Binding TableForBinding, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" CanUserAddRows="False" 
                  FrozenColumnCount="2" Margin="5,10" SelectionMode="Single" SelectionUnit="Cell" IsReadOnly="{Binding IsReadOnly}"/>
    </DockPanel>
</Window>
