<Window x:Class="Iit.Fibertest.Client.MyMessageBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:client="clr-namespace:Iit.Fibertest.Client"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" d:DataContext="{d:DesignData ConfirmationViewModel}">
    <StackPanel>
        <ItemsControl ItemsSource="{Binding Lines}" Margin="100,25,100,50">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="client:MyMessageBoxLineModel">
                    <TextBlock Text="{Binding Line}" TextAlignment="Center" FontWeight="{Binding FontWeight}" Foreground="{Binding Foreground}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10,0,0,10">
            <Button Name="OkButton" Content="OK" Margin="5" HorizontalAlignment="Center" Width="100"/>
            <Button Name="CancelButton" Visibility="{Binding CancelVisibility}" Content="Cancel" Margin="5" HorizontalAlignment="Center" Width="100"/>
        </StackPanel>
    </StackPanel>
</Window>
