<Window x:Class="Iit.Fibertest.Client.AccidentsReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
         ResizeMode="NoResize" WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight"
        d:DataContext="{d:DesignData AccidentsReportViewModel}">
    <StackPanel>
        <RadioButton GroupName="1" Content="Current accidents" Margin="5" IsChecked="True"/>
        <RadioButton GroupName="1" Content="Accidents for period" Margin="5" IsChecked="{Binding IsCustomReport}"/>
        <Border Margin="5" BorderThickness="1" BorderBrush="Gray" IsEnabled="{Binding IsCustomReport}">
            <StackPanel Margin="5">
                <ComboBox Width="120" Height="22" Margin="5"></ComboBox>
            </StackPanel>
        </Border>
        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
            <Button Name="CreateReport" Content="Create report" Margin="5" Width="120" Height="22"/>
            <Button Name="Cancel" Content="Cancel" IsCancel="True" Margin="5" Width="120" Height="22"/>
        </StackPanel>
    </StackPanel>
</Window>
