<Window x:Class="Iit.Fibertest.RtuWpfExample.OtauView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Iit.Fibertest.RtuWpfExample"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        d:DataContext="{d:DesignInstance local:OtauViewModel}">
    <StackPanel>
        <DockPanel Margin="0,10,0,0">
            <TextBlock Text="Ip address" Margin="5" DockPanel.Dock="Left" Width="100" TextAlignment="Right"/>
            <TextBox Margin="5" Text="{Binding IpAddress}" HorizontalAlignment="Stretch"/>
        </DockPanel>
        <DockPanel>
            <TextBlock Text="Tcp port" Margin="5" DockPanel.Dock="Left" Width="100" TextAlignment="Right"/>
            <TextBox Margin="5" Text="{Binding OtauTcpPort}" HorizontalAlignment="Stretch"/>
        </DockPanel>
        <Button Name="InitOtau" Content="Initialize" Margin="5"/>
        <TextBlock Margin="5" Background="AliceBlue" Text="{Binding CharonInfo}" Padding="5,0,0,0" />
        <DockPanel Margin="0,30,0,10">
            <TextBlock Margin="5" Background="AliceBlue" Text="{Binding ActivePortMessage}" DockPanel.Dock="Bottom" Padding="5,0,0,0"/>
            <ComboBox Margin="5" Width="150" DockPanel.Dock="Left" ItemsSource="{Binding OtauList}" SelectedItem="{Binding SelectedOtau}"/>
            <TextBox Margin="5" Width="33" DockPanel.Dock="Left" Text="{Binding NewActivePort}"/>
            <Button Name="SetActivePort" Margin="5" Content="Set active optical port" Width="150" IsEnabled="{Binding IsOtauInitialized}"/>
        </DockPanel>
        <GroupBox Header="Attach" Margin="5">
            <StackPanel>
                <DockPanel Margin="5">
                    <TextBlock Text="Bop ip address" Margin="0,0,10,0"/>
                    <TextBox Width="100" Text="{Binding BopIpAddress}" Margin="0,0,10,0"/>
                    <TextBlock Text="Bop tcp port" Margin="0,0,10,0"/>
                    <TextBox MinWidth="45" Text="{Binding BopTcpPort}"/>
                </DockPanel>
                <DockPanel Margin="5">
                    <TextBlock Text="Main charon" Margin="0,0,10,0" DockPanel.Dock="Left"/>
                    <TextBlock Text="{Binding MainCharonNetAddress}" Margin="0,0,10,0" DockPanel.Dock="Left"/>
                    <TextBlock Text="Master optical port" Margin="0,0,10,0" DockPanel.Dock="Left"/>
                    <TextBox MinWidth="45" Text="{Binding PortForAttachment}"/>
                </DockPanel>
                <Button Name="AttachOtau" Content="Attach" Margin="5" IsEnabled="{Binding IsOtauInitialized}"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Detach" Margin="5">
            <StackPanel>
                <DockPanel>
                    <ComboBox Margin="5" Width="150"/>
                    <TextBlock Text="from optical port "  Margin="5"/>
                </DockPanel>
                <Button Name="DetachOtau" Content="Detach" Margin="5" IsEnabled="{Binding IsOtauInitialized}" />
            </StackPanel>
        </GroupBox>
    </StackPanel>
</Window>
