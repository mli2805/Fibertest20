<UserControl x:Class="Setup.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:setup="clr-namespace:Setup"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance setup:HeaderViewModel}">
    <Border BorderThickness="0,1,0,1" BorderBrush="LightGray" Height="60">
        <DockPanel >
            <Image DockPanel.Dock="Right" Width="60" Margin="0,0,20,0"></Image>
            <TextBlock DockPanel.Dock="Top" Margin="5" FontWeight="Bold" Text="{Binding InBold}"/>
            <TextBlock Margin="15,5,5,5" Text="{Binding Explanation}"/>
        </DockPanel>
    </Border>
</UserControl>
