<UserControl x:Class="Iit.Fibertest.Uninstall.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:uninstall="clr-namespace:Iit.Fibertest.Uninstall"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance uninstall:HeaderViewModel}">
    <Border BorderThickness="0,1,0,1" BorderBrush="LightGray" Height="50">
        <DockPanel >
            <Image DockPanel.Dock="Right" Source="../../Resources/RtuNotAccessible.png" Height="28" Margin="0,0,20,0"/>
            <TextBlock DockPanel.Dock="Top" Margin="15,5,5,2" FontWeight="Bold" Text="{Binding InBold}"/>
            <TextBlock Margin="25,0,5,5" Text="{Binding Explanation}"/>
        </DockPanel>
    </Border>
</UserControl>
