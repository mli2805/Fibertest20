import { MonitoringModePipe } from './monitoring-mode.pipe';

describe('MonitoringModePipe', () => {
  it('create an instance', () => {
    const pipe = new MonitoringModePipe();
    expect(pipe).toBeTruthy();
  });
});
