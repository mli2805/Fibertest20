<UserControl x:Class="TreeVision.TreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:treeVision="clr-namespace:TreeVision"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance treeVision:TreeViewModel}">
    <Grid>
        <Border BorderBrush="BlueViolet" BorderThickness="2"  Width="300">
            <ListBox ItemsSource="{Binding Rtus}">

            </ListBox>
        </Border>
    </Grid>
</UserControl>
