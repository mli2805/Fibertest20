<UserControl x:Class="Iit.Fibertest.Install.InstTypeChoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:install="clr-namespace:Iit.Fibertest.Install"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance setup:InstTypeChoiceViewModel}">
    <DockPanel>
        <install:HeaderView DockPanel.Dock="Top" DataContext="{Binding HeaderViewModel}"/>
        <TextBlock DockPanel.Dock="Top" Margin="15,5" Text="{Binding Text1}"/>
        <StackPanel DockPanel.Dock="Top" Margin="15,5" Orientation="Horizontal">
            <TextBlock Margin="0,5,5,5" Text="{Binding Text2}"/>
            <ComboBox Margin="10,5,5,5" Width="150" ItemsSource="{Binding InstTypes}" SelectedItem="{Binding SelectedType, Mode=TwoWay}"/>
        </StackPanel>

        <StackPanel Margin="10,30,5,5" Visibility="{Binding MySqlTcpPortVisibility}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="5" Text="MySql TCP port"/>
                <TextBox Margin="5" Text="{Binding MySqlTcpPort}" Width="50"/>
            </StackPanel>
            <StackPanel Margin="0,10,0,0">
                <CheckBox Content="Install Web Service" IsChecked="{Binding IsWebNeeded}" Margin="5,0,0,0"></CheckBox>
                <GroupBox DockPanel.Dock="Bottom" Header="Select path to the archive with web components" Margin="0,5,0,0" IsEnabled="{Binding IsWebNeeded}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Bottom" Text="Archive should be selected if option checked" 
                                   Margin="30,0,0,0" Visibility="{Binding ArchiveErrorMessageVisibility}"/>
                        <Button cal:Message.Attach="Browse" DockPanel.Dock="Right" 
                                Margin="5" Width="80" Content="{x:Static stringResources:Resources.SID_Browse}"/>
                        <TextBlock Text="{Binding WebArchivePath}" Margin="10,5"/>
                    </DockPanel>
                </GroupBox>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</UserControl>
