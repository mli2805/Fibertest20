<UserControl x:Class="Iit.Fibertest.Install.InstallationSettingsForSuperClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DataContext="{d:DesignData InstallationSettingsForSuperClientViewModel}">
    <StackPanel Margin="5">
        <TextBlock Text="{Binding R0}" Margin="10"/>
        <RadioButton GroupName="1" Content="{Binding R1}" Margin="10, 5" IsChecked="{Binding IsR1Checked}"/>
        <RadioButton GroupName="1" Content="{Binding R2}" Margin="10, 5"/>
    </StackPanel>
</UserControl>
