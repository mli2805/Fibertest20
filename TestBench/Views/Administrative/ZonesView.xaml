<Window x:Class="Iit.Fibertest.TestBench.ZonesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
        xmlns:testBench="clr-namespace:Iit.Fibertest.TestBench"
        mc:Ignorable="d"
        Height="300" SizeToContent="Width" WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance testBench:ZonesViewModel}">
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom"  Orientation="Horizontal">
            <Button Name="Save" Content="{x:Static stringResources:Resources.SID_Save}" IsDefault="True" Width="100" Margin="30,20,20,15"/>
            <Button Name="Cancel" Content="{x:Static stringResources:Resources.SID_Cancel}" IsCancel="True" Width="100" Margin="0,20,0,15"/>
        </StackPanel>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Rows}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title"  Width="120" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Comment"  Width="330" Binding="{Binding Comment}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
