<Window x:Class="Iit.Fibertest.TestBench.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:testBench="clr-namespace:Iit.Fibertest.TestBench"
    Width="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button Width="120" Height="25" Name="AddOneNode" Content="Add One Node" />
            <Button Width="120" Height="25" Name="Populate" Content="Populate" />
        </StackPanel>
        <GroupBox Grid.Column="1" Name="MapBox">
            <testBench:MapUserControl DataContext="{Binding GraphVm}"/>
        </GroupBox>
    </Grid>

</Window>