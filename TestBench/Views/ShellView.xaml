<Window x:Class="Iit.Fibertest.TestBench.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:testBench="clr-namespace:Iit.Fibertest.TestBench"
        xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
        Height="900" Width="1600">

    <DockPanel>
        <testBench:MainMenuView DockPanel.Dock="Top"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" MinWidth="80"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <testBench:LeftPanelView DataContext="{Binding MyLeftPanelViewModel}"/>
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Left"/>
            <TabControl Grid.Column="2">
                <TabItem Header="{x:Static stringResources:Resources.SID_System_events }">
                    <StackPanel>
                        <Button Width="120" Height="25" Name="Save" Content="Save Db" />
                    </StackPanel>
                </TabItem>
                <TabItem Header="{x:Static stringResources:Resources.SID_Graph_of_traces}" IsSelected="True">
                    <testBench:MapUserControl DataContext="{Binding GraphReadModel}"/>
                </TabItem>
            </TabControl>
        </Grid>

    </DockPanel>

</Window>