<Window x:Class="Iit.Fibertest.TestBench.TraceChoiceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:testBench="clr-namespace:Iit.Fibertest.TestBench"
        mc:Ignorable="d"
        SizeToContent="Height" Width="500" WindowStartupLocation="CenterScreen">
    <StackPanel>
        <TextBlock Text="{Binding Caption}"  TextWrapping="Wrap" Margin="5, 20, 5,20" />
        <ItemsControl ItemsSource="{Binding Choices}" Margin="5,5,5,20">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="testBench:RadioButtonModel">
                    <CheckBox Content="{Binding Title}" IsChecked="{Binding IsChecked, Mode=TwoWay}" IsEnabled="{Binding IsEnabled}" Margin="5"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Orientation="Horizontal">
            <Button Name="Accept"  Content="Добавить оборудование" Margin="5" HorizontalAlignment="Center" Width="200"/>
            <Button Name="Cancel"  Content="Не добавлять оборудование" Margin="5" HorizontalAlignment="Center" Width="200"/>
        </StackPanel>

    </StackPanel>
</Window>
