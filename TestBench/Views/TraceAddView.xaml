<Window x:Class="Iit.Fibertest.TestBench.TraceAddView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:stringResources="clr-namespace:Iit.Fibertest.StringResources;assembly=Iit.Fibertest.StringResources"
        xmlns:testBench="clr-namespace:Iit.Fibertest.TestBench"
        mc:Ignorable="d"
        Title="TraceAddView" SizeToContent="Height" Width="300" WindowStartupLocation="CenterScreen">
    <StackPanel>
        <GroupBox Header="Information" Margin="5">
            <StackPanel>
                <DockPanel>
                    <TextBlock Margin="5" Text="{x:Static stringResources:Resources.SID_Title}" />
                    <TextBox Margin="5" 
                             Text="{Binding Title, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}" />
                </DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="5" Text="RTU" />
                    <TextBlock Margin="5" Text="{Binding RtuTitle}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="5" Text="Port" />
                    <TextBlock Margin="5" Text="{Binding PortNumber}" />
                </StackPanel>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Nodes" Margin="5">
            <ItemsControl ItemsSource="{Binding NodesStatistics}" Margin="5,5,5,20">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="testBench:NodesStatisticsItem">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="5" Width="150" HorizontalAlignment="Right" Text="{Binding NodeType}" />
                            <TextBlock Margin="5" Text="{Binding Count}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>
        <GroupBox Header="Trace mode" Margin="5">
            <StackPanel Orientation="Horizontal">
                <RadioButton GroupName="1" Name="Dark" Margin="35,5,5,5" Content="Dark OF"/>
                <RadioButton GroupName="1" Name="Light" Margin="35,5,5,5" Content="Light OF" IsChecked="{Binding IsTraceModeLight, Mode=TwoWay}"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Comment" Margin="5">
                <TextBox Margin="5" Text="{Binding Comment}"/>
        </GroupBox>
        <StackPanel Orientation="Horizontal" Margin="15,15">
            <Button Name="Save" Height="25" Width="100" Margin="5" Content="{x:Static stringResources:Resources.SID_Save}" IsEnabled="{Binding IsButtonSaveEnabled}"/>
            <Button Name="Cancel" Height="25" Width="100" Content="{x:Static stringResources:Resources.SID_Cancel}"/>
        </StackPanel>

    </StackPanel>
</Window>
